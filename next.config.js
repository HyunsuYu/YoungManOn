/** @type {import('next').NextConfig} */
// Vercel 동적 호스팅: 서버(Route Handler)가 요청 시점에 온통청년 API를 호출합니다.
// GitHub Pages용 정적 export/basePath 설정은 제거했습니다.
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
